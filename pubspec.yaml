name: riverpod_camera_demo1
description: A new Flutter project.

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=2.18.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  equatable: ^2.0.5
  camera: ^0.10.1
  flutter_riverpod: ^2.1.3

dev_dependencies:

  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

  # camera: ^0.10.1
  # flutter_riverpod: ^2.1.3

  path:
  path_provider:
  riverpod: ^2.1.3

flutter:

  uses-material-design: true
